# nixpacks.toml
# LibreOffice removed from build — not used in production pipeline.
# Add back when Phase 3 LibreOffice rendering is needed:
#   [phases.setup]
#   aptPkgs = ["libreoffice", "libreoffice-writer"]
